<% if user.attended_events.include?(event)%>
  <p>Attended</p>
<% else %>
  <%= button_to (user == current_user ? "Attend event" : "Invite to event"), signups_path(event), method: :post, params: { signup: { attendee_id: user.id, attended_event_id: event.id } } %>
<% end %>
