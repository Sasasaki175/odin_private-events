<p style="color: green"><%= notice %></p>

<%= render @event %>
<h2>Attendees</h2>
<% @event.attendees.each do |attendee| %>
  <%= attendee.email %>
  <br>
<% end %>

<div>
  <%= link_to "Back to events", events_path %>
</div>
