<% content_for :title, "Editing '#{@event.name}'" %>

<h1>Editing '<%= @event.name %>'</h1>

<%= render "form", event: @event %>

<br>

<div>
  <%= link_to "Back to event details", @event %><br>
  <%= link_to "Back to events", events_path %>
</div>
